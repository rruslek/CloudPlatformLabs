apiVersion: v1
kind: Service
metadata:
  name: imagic

spec:
  type: LoadBalancer
  selector:
    app: imagic
  ports:
    - protocol: TCP
      name: http-traffic
      port: 8080
      targetPort: 8080